1. ases.php,csstable.css,csstable1.css and report.html are to placed in the root folder of the localhost server.

2. Change the location of ases.py in the ases.php where the LBS folder is located.

3. Inside ases.py Change the location of the function genfromtxt which takes the training and testing set

4. If there is no python install Python 2.7 along with numpy,scipy & scikitlearn libraries.